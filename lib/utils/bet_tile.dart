import 'package:flutter/material.dart';

class BetTile extends StatelessWidget {
  const BetTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
